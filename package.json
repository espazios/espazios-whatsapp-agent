{
  "name": "espazios-whatsapp-agent",
  "version": "0.1.0",
  "private": true,
  "description": "Agente de ventas IA por WhatsApp para Espazios (remodelacion y carpinteria a medida).",
  "type": "module",
  "scripts": {
    "generate-quote": "tsx scripts/generate-quote.ts",
    "dev": "tsx watch src/server.ts",
    "start": "tsx src/server.ts",
    "typecheck": "tsc --noEmit"
  },
  "dependencies": {
    "@kapso/whatsapp-cloud-api": "^0.3.0",
    "dotenv": "^16.4.5",
    "fastify": "^5.1.0",
    "googleapis": "^144.0.0"
  },
  "devDependencies": {
    "@types/node": "^22.5.0",
    "tsx": "^4.19.0",
    "typescript": "^5.6.0"
  }
}
